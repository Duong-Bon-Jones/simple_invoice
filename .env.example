# 101 Digital identity server (server-side only, never NEXT_PUBLIC_)
IDENTITY_BASE_URL=https://identity.101digital.io/t/101digital.core
OAUTH_CLIENT_ID=your_client_id
OAUTH_CLIENT_SECRET=your_client_secret

# 101 Digital backend API base (membership-service and invoice-service both live under this base)
API_BASE_URL=https://api.101digital.io
